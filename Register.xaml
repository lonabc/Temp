<Window x:Class="WpfAppLogin.Register"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
          xmlns:hc="https://handyorg.github.io/handycontrol"
        xmlns:local="clr-namespace:WpfAppLogin"
        mc:Ignorable="d"
        Title="Register" Height="600" Width="910">
    <Window.Resources>
        <Style x:Key="Text" TargetType="TextBlock">
            <Setter Property="Margin" Value="5,12,2,2"/>
        </Style>

        <Style x:Key="regeisterText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="Blue"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="Margin" Value="0,10,0,0"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0"  Background="BurlyWood" Height="30px">
            <TextBlock HorizontalAlignment="Center"  TextWrapping="Wrap" Text="智能家居管理系统" Foreground="AliceBlue"  VerticalAlignment="Center"/>
        </Grid>
        <Grid Grid.Row="1" Background="White" Width="400" Margin="250,51,250,172" Height="280" HorizontalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Border BorderBrush="Black" BorderThickness="2,2,2,0"   Grid.Row="0">
                <Grid Grid.Row="0" Background="Cyan" Width="Auto"  Height="30">
                    <TextBlock  HorizontalAlignment="Center"  TextWrapping="Wrap" Text="注册" VerticalAlignment="Center" />
                </Grid>
            </Border>

            <Border BorderBrush="Black" BorderThickness="2,0,2,0"   Grid.Row="1">
                <Grid Grid.Row="1" Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel HorizontalAlignment="Right" Width="auto">
                        <TextBlock Style="{StaticResource Text}" Margin="7,10,2,5" Text="用户名"/>
                        <TextBlock Style="{StaticResource Text}" Text="密码"/>
                        <TextBlock Style="{StaticResource Text}" Margin="5,15,2,2" Text="确认密码"/>
                        <TextBlock Style="{StaticResource Text}"  Text="邮箱"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" HorizontalAlignment="Left" Width="200">
                        <TextBox x:Name="txtUserName" Text="{Binding  name}" Width="180" Margin="10,2,0,2"/>
                        <TextBox x:Name="txtUserPassword" Text="{Binding password}" Width="180" Margin="10,2,0,2"/>
                        
                     
                        <TextBox x:Name="txtvfCode" Text="{Binding  verificationCode}" Width="180" Margin="10,2,0,2"/>
                        <TextBox x:Name="txtEmail" Text="{Binding  email}" Width="180" Margin="10,2,0,2"/>
                    </StackPanel>
                  
                </Grid>
            </Border>
            <Border BorderBrush="Black" BorderThickness="2,0,2,2"  CornerRadius="0"  Grid.Row="2">
                <Grid Height="90">
                    <StackPanel>
                        <Button Style="{StaticResource ButtonSuccess}"  x:Name="btnLogin" Click="btnRegister_Click" Content="注册"  Width="180" Margin="0,40,0,0" />
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
